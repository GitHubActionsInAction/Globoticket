name: "test"
permissions:
  actions: write
  packages: write
  contents: read
on:
  workflow_dispatch:

env:
  deploymentfileFolder: 'deployment-automation'
  GH_TOKEN: ${{ github.token }} #required for gh tool

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Retrieve latest packages
        run: |
          export frontend_version=$(gh api  /user/packages/container/frontend/versions | jq '.[0].metadata.container.tags[0]')
          echo $frontend_version